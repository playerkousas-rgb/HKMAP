# 防增肥規範（Scout System · HKMAP）

> **一句話：呢個專案係「純靜態、零依賴、零打包」。所有規則都為咗守住呢三樣，令 Vercel 儲存空間永遠唔會爆。**
> 改版前請先睇「✅ 改版 Checklist」一節。有疑問嘅話，`npm run verify` 會直接答你。

---

## 1. 鐵律（每次改版都要守）

| # | 規則 | 點把關 |
| --- | --- | --- |
| 1 | **`dependencies` 永遠係 `{}`** — 唔准裝執行期套件（Vite／Tailwind／jQuery／icon 套件…全部唔要）。要用第三方庫只可以 CDN（見 `index.html` 的 Leaflet、proj4js）或者自己寫。 | `npm run build` 會讀 `package.json`，一有 runtime dep 即 fail |
| 2 | **`devDependencies` 亦係 `{}`** — 檢查工具（check／lint／build）用 Node 內建模組寫，唔裝 ESLint／Prettier。真係要加建置工具，一定要放 `devDependencies`，唔可以放 `dependencies`。 | 同上 |
| 3 | **唔加打包器、唔產生 `dist/`** — Vercel 直接食根目錄（`outputDirectory` 用預設＝根目錄），部署內容＝source，冇重複建置產物。 | `npm run build` 會列出「實際上傳清單」 |
| 4 | **每個檔案都要有人引用** — 新圖片／字型／資料檔一定要被 HTML／CSS／JS 引用。孤立檔案 > 8 KB 直接 fail。 | `npm run check`（死檔檢查） |
| 5 | **大檔唔准入 repo** — 單一檔案 ≤ 400 KB、整個部署 ≤ 1.5 MB。原稿（PSD／AI／Sketch／高解析截圖／示範圖）一律唔可以 commit，要放就放外部儲存（Google Drive 等），網頁只引用最終壓縮版。 | `npm run check`（預算檢查，見 `scripts/lib.mjs` 的 `BUDGET`） |
| 6 | **備份／暫存檔一律唔准出現** — `*.bak`、`*.tmp`、`*.old`、`*.log`、`uploads/`、`tmp/`、`草稿/` 已經寫入 `.gitignore` 同 `.vercelignore`；`npm run lint` 見到會直接 fail。 | `npm run lint` |
| 7 | **每次改版都要跑 `npm run verify`** 先 commit／部署。 | 見第 4 節 |

> ⚠️ 想放寬預算？唔好靜靜改 `BUDGET`。請先量度：`npm run build` 會印出每個檔案大小同 gzip 後總量，用數據講點解要放寬。

---

## 2. 檔案擺位規範

```
index.html          ← 主頁（設計＝列印）
download.html       ← 電子地圖下載中心
mock.html           ← 新使用者 MOCK／四步教學
print.html          ← 舊網址 /print 轉址（刻意保留，唔好刪：有人 bookmark）
css/app.css         ← 主介面＋列印樣式（列印規則集中在檔尾）
css/download.css    ← 下載頁樣式
css/mock.css        ← 教學頁樣式
js/app.js           ← 地圖、CP、比例、列印、本機暫存（唯一入口模組）
js/hkgeo.js         ← 測地／UTM 方格／比例／紙張幾何（純函式，可以在 Node 直接測）
js/overprint.js     ← 紫紅套印路線同 CP 圖示
js/catalog.js       ← 官方下載資料（政府連結、圖幅索引）
js/download-page.js ← 下載頁互動
assets/             ← 只放細小矢量圖（現時只有 mark.svg，775 B）
scripts/            ← 檢查工具（唔會被網站引用；見第 3 節，唔可以排除上傳）
教學.md             ← 文字教學，**網站有連結，所以一定要部署**
防增肥規範.md / README.md ← repo 文件，唔需要部署（已在 .vercelignore）
```

**加新頁面時：** 記得喺 `mock.html`／`index.html`／`download.html` 其中一處加入口連結。`npm run check` 會檢查所有本機連結唔可以死（引用咗唔存在嘅檔＝fail）。

**放 CSS／JS：** 唔好為一個小功能新開檔案。現時 5 個 JS、3 個 CSS 已經係上限；新檔案要有清楚理由（例如「純函式可以獨立測試」）。

---

## 3. `.vercelignore` 運作同禁區

`.vercelignore` 決定「邊啲檔案唔會上傳」。目前已排除：`.git`、`node_modules`、各類快取／建置資料夾（`dist`、`build`、`.next`、`.cache`…）、備份暫存檔（`*.bak`／`*.tmp`／`*.old`／`*.log`）、測試上傳資料夾（`uploads/`、`tmp/`、`草稿/`）、以及唔需要部署嘅 repo 文件（`README.md`、`防增肥規範.md`）。

### 🚫 絕對唔可以加入排除清單
| 檔案 | 原因 |
| --- | --- |
| `index.html`／`css/`／`js/`／`assets/` | 就係網站本身 |
| `教學.md` | `mock.html` 有連結，排咗會 404 |
| `vercel.json` | 部署設定（headers／buildCommand） |
| `package.json`／`package-lock.json`／`scripts/` | 雲端執行 `npm run build` 要用；排咗建置會失敗 |

`npm run build` 會用模擬路徑測試 `.vercelignore`（例如 `.git/config`、`node_modules/...`、`x.bak`、`uploads/a.jpg` 必須擋；`js/app.js`、`教學.md` 必須留），改壞設定會即刻報錯。

---

## 4. 檢查工具（零依賴）

| 指令 | 做乜 | 幾時用 |
| --- | --- | --- |
| `npm run check` | 語法（`node --check`）、測地／方格／比例單元測試、套印邏輯、官方資料完整性、本機連結、DOM id 接線、死檔、部署預算 | 每次改動後 |
| `npm run lint` | 除錯殘留（`console.*`／`debugger`）、`eval`、`http://`、寫死 `localhost`、**使用者輸入放入 HTML 前未 `esc()`**、重複 `class` 屬性、HTML meta／`img alt`／`target=_blank` 冇 `noopener`、死 CSS class（警告）、殘留測試／備份檔 | 每次改動後 |
| `npm run smoke` | 用最小 DOM／Leaflet 替身真正開一次 app：放 △ ○ ◎、拖移、返回／重做、鎖比例、改紙張、白紙外警告、列印、方格跳轉、搜尋、切底圖、清除暫存（唔需要瀏覽器） | 改動 `js/` 之後 |
| `npm run build` | 跑齊 check（含 smoke）＋lint，再驗證 `vercel.json`／`.vercelignore`／`package.json`，最後印出「實際上傳清單＋容量」 | 部署前 |
| `npm run verify` | 三個一次跑齊（**最少要跑呢個**） | commit／部署前 |
| `npm run serve` | `python3 -m http.server 8080` 本機預覽 | 手動驗收 |

**關於警告：** 死 CSS class 只會警告唔會 fail。見到警告就順手刪（`npm run lint` 會講係邊個檔案、邊個 class）。

**關於 `npm run build`：** 佢**唔會**產生任何檔案，只係守門員。Vercel 上嘅建置指令已經寫死 `npm run build`，所以每次部署都會自動跑齊檢查；檢查唔過＝部署唔會上線（呢個係故意嘅）。

---

## 5. `vercel.json` 設定說明（唔好亂改）

```json
{ "cleanUrls": true, "trailingSlash": false,
  "buildCommand": "npm run build", "installCommand": "npm install",
  "outputDirectory": "." }
```

- **`outputDirectory: "."` 一定要保留（load-bearing，唔好刪）** —— 呢個係 2026-09 一次真實部署失敗搵出嚟嘅：
  未加 `buildCommand` 之前，Vercel 會**跳過建置步驟**，直接由 repo 根目錄出檔（所以一直冇問題）。
  一旦有 build command，Vercel 就會行 static-build pipeline，並要求 **Output Directory 存在**；
  本網站冇 `public/`，而 dashboard 嘅 Output Directory 可能仲留住舊預設值（`public`），
  結果就會出現 `No Output Directory named "public" found after the Build completed`
  / `STATIC_BUILD_NO_OUT_DIR`，部署直接失敗。
  `vercel.json` 寫死 `"."`（＝專案根目錄）就覆蓋 dashboard 設定，唔會再漂移。
  `npm run build` 會驗證呢個值，刪咗會即刻 fail（防止將來「清理」時誤刪）。
- **`headers` 只加 `X-Content-Type-Options`、`Referrer-Policy`**。
  - ❌ 唔好加 `X-Frame-Options` 或 CSP `frame-ancestors`：Arena 預覽係用 iframe 嵌入，加咗會白畫面。
  - ❌ 唔好加 `Cache-Control: immutable`：檔名冇 hash（`app.js` 唔係 `app.abc123.js`），長快取會令改版後用戶仍然攞舊檔。用 Vercel 預設（ETag＋revalidate）最安全。
  - ❌ 唔好加 CSP 白名單：地圖圖磚（`mapapi.geodata.gov.hk`）、字型（`fonts.gstatic.com`）、CDN（`unpkg`、`cdnjs`）、OpenTopoMap 全部要放行，寫死會爛圖。

---

## 6. 外部依賴（CDN）政策

| 依賴 | 用途 | 政策 |
| --- | --- | --- |
| Leaflet 1.9.4（unpkg） | 地圖引擎 | **版本要 pin 死**，唔可以用 `@latest`（改版會靜靜壞掉） |
| proj4js 2.11.0（cdnjs） | UTM 轉換 | 同上 |
| Google Fonts（Noto Sans TC／Outfit） | 字型 | 只加 `<link>`，唔可以下載字型入 repo（Noto Sans TC 全套＝幾 MB） |
| LandsD Map API／OpenTopoMap | 圖磚 | 使用者瀏覽器直接讀，伺服器零頻寬、零快取 |

**換 CDN／改版本前**：一定要手動開 `index.html` 睇地圖有冇正常出圖，因為呢啲檢查（Node）測唔到 CDN 行為。

---

## 7. 刻意保留（唔係死重，唔使「清理」）

- `print.html`（771 B）：舊網址 `/print` 仍會轉去設計頁，刪咗會 404。
- `教學.md`（7.2 KB）：網站有連結，所以要部署。
- `scripts/` 4 個檔案（約 43 KB）：體積係零頭，但係防增肥守門員；排除咗雲端建置會失敗。
- `css/app.css` 尾段嘅命名 `@page` 規則（`@page pageA4L`…）：舊版瀏覽器唔支援 `page:` 屬性時嘅 fallback，唔好刪。

---

## 8. 容量現況（2026-09 實測）

| 項目 | 數值 |
| --- | --- |
| 實際上傳到 Vercel | **24 個檔案、239.1 KB**（gzip 後 83.9 KB） |
| 其中網站本身（HTML／CSS／JS／assets／教學.md） | 158.7 KB |
| 其中檢查工具 `scripts/` 6 個檔（部署守門員） | 77.9 KB |
| 其中設定（`vercel.json`／`package.json`／lock／`.vercelignore`） | 2.5 KB |
| 執行期依賴 | **0 個** |
| 建置產物 | **0 個**（冇 `dist/`） |
| `.vercelignore` 擋住 | `README.md`、`防增肥規範.md`、`.gitignore`（15.0 KB；另加 `.git` 約 84 KB 唔會上傳） |
| node_modules | 唔存在（零依賴，所以連安裝都係幾百毫秒） |

> **想再瘦？** 可以將 `scripts/` 加入 `.vercelignore` 並刪咗 `vercel.json` 嘅 `buildCommand`（慳 77.9 KB），代價係冇咗「部署前自動檢查」呢道閘。除非 Vercel 帳戶真係爆配額，否則**唔建議**。

每次改版後用 `npm run build` 重新量度，如果數字大幅上升，先問「係邊個檔案增肥、有冇人引用、可唔可以壓縮或者放外部」。

---

## ✅ 改版 Checklist

1. [ ] 有冇裝新套件？ → `dependencies` 必須仍然係 `{}`。
2. [ ] 有冇新檔案？ → 有冇被 HTML／CSS／JS 引用？大唔大過 400 KB？
3. [ ] 有冇截圖／原稿／示範圖放咗入 repo？ → 改放外部儲存，只引用壓縮版。
4. [ ] 有冇 `.bak`／`.tmp`／測試檔殘留？ → 刪。
5. [ ] 使用者文字放入 HTML 前有冇 `esc()`？（`npm run lint` 會捉）
6. [ ] 跑咗 `npm run verify` 未？三個都要 pass。
7. [ ] 改咗 `.vercelignore`／`vercel.json`？ → `npm run build` 會驗證，唔好排除第 3 節禁區檔案。
8. [ ] 部署後手動開一次：主頁地圖出圖、放一個 CP、按「列印地圖」睇列印預覽、`download.html` 三個分頁、`mock.html`。
